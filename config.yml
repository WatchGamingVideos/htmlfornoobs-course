title: HTMLfornoobs
description: HTML for starters
template:
  repo: htmlfornoobs-template
  name: htmlfornoobs
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
